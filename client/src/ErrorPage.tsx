const ErrorPage = (props: any) => {
    const { error } = props;

    return (
        <div id="error-page">
            <p>Sorry, an unexpected error has occurred.</p>
        </div>
    )
}


export default ErrorPage;